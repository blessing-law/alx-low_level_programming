#include <stdio.h>
#include "main.h"
#include <string.h>

/**
 * _strcat - concatenate two strings
 * @dest: first string
 * @src: second string
 *
 * Return: pointer to resulting string
 */

char *_strcat(char *dest, char *src)
{
	strcat(dest, src);
	return (0);
}
