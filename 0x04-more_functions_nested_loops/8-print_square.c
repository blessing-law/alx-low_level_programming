#include "main.h"
#include <stdio.h>

/**
 * print_square - print a square
 *
 * @size: the size of the square
 * Return: 0
 */

void print_square(int size)
{
	int i = 0;
	int j;

	while (i <= n)
	{
		j = 0;

		while (j <= n)
		{
			_putchar('#');
			_putchar(' ');
		}
		_putchar('#');
		_putchar('\n');
	}
	return (0);
}
