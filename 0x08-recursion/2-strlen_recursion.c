#include <stdio.h>
#include <string.h>
#include "main.h"

/**
 * _strlen_recursion - find length of string
 * @s: input string
 *
 * Return: string length
 */

int _strlen_recursion(char *s)
{
	int l;

	l = strlen(s);
	return (l);
}
