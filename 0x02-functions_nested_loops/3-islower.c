#include "main.h"
#include <stdio.h>

/**
 *_islower - checks for lowercase letter
 *@c: Character to be checked
 *
 * Return: 1 if true
 */

int islower(int c);

int _islower(int c)
{
	if (islower(c))
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
